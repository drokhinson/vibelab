fastapi>=0.111.0
uvicorn[standard]>=0.29.0
supabase>=2.4.0,<2.26.0  # cap: 2.26+ adds pyiceberg (C ext, no 3.14 wheels yet)
python-dotenv>=1.0.0
bcrypt>=4.1.0
PyJWT>=2.8.0
python-multipart>=0.0.9
httpx[http2]>=0.27.0  # http2 extra: db.py builds the PostgREST transport itself (h2 GOAWAY retry)
cryptography>=42.0.0
pywebpush>=2.0.0  # Web Push (VAPID + aes128gcm) for BoardgameBuddy notifications
truststore>=0.9.0  # use OS certificate store (fixes SSL errors on Windows/corporate proxies)
