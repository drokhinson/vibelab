"""
routes/daywordplay/constants.py

User identity is now handled by Supabase Auth (verified via shared
shared-backend/jwt_auth.py); no per-app JWT secret is required.
This module is kept for future lookup tables / enums.
"""
